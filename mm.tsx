 {/* <div className={`mt-20 transform transition-all duration-1000 delay-500 ${isLoaded ? 'translate-y-0 opacity-100' : 'translate-y-20 opacity-0'}`}>
            <div className="flex justify-center space-x-4 mb-8 auto">
              {[0, 1, 2].map((index) => (
                <div 
                  key={index}
                  className="w-20 h-12 rounded-lg overflow-hidden cursor-pointer transform hover:scale-110 transition-transform"
                  onClick={() => setCurrentProject(index)}
                >
                  <img 
                    src={projects[index].image} 
                    alt="" 
                    className="w-full h-full object-cover"
                  />
                </div>
              ))}
            </div>
          </div> */}